<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.afifah.uas_afifah_yasmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UAS_Afifah_Yasmin">
        <activity
            android:name=".MainMenu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AlertToast" />
        <activity android:name=".Camera"/>
        <activity android:name=".Asyntask"/>
        <activity android:name=".Maps"/>
        <activity android:name=".SMS" />
        <activity android:name=".TabViewActivity" />
        <activity android:name=".LocalDB" />
        <activity android:name=".SharePreff" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".UserActivity" />

   </application>

</manifest>